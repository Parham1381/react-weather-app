To build the project after unzipping the zip file:
1) Under app-server/constant/api-key.json put your api key value
1) Open the app-server folder and run "npm start" to start the node.js on port 3000
2) Open the web application and run "npm start" on the main app